# Archived architecture note

This document describes a retired workstation harness and is kept only to
explain historical result files in this directory. It is not an implementation
or setup guide. The supported architecture is Pi + Rapid-MLX on loopback with
the Qwen3.6 model; see [`README.md`](../../README.md), [`install.sh`](../../install.sh),
and [`doctor.sh`](../../doctor.sh) at the repository root.
