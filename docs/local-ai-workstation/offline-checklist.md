# Archived offline checklist

The former workstation checklist is retired and must not be used to select a
runtime. For the supported local-only setup, run `./doctor.sh` and follow the
root [`README.md`](../../README.md). It checks Apple Silicon, memory, disk,
Pi, Rapid-MLX, the loopback server, and model readiness.
